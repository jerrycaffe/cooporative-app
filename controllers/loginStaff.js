const staff = require('../database/model/addStaff')

module.exports = (req, res)=>{
    //try to find the user 
    //check the password
    //if user password is correct
    //check if the user has an admin token 
    //if the user have admin token then the admin dashboard should be serveredirect the user to the homepaged to the user
    //else check if the user has login access
    //if not then 
    
    res.redirect('/')
}